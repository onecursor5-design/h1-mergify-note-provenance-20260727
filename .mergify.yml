pull_request_rules:
  - name: h1lab docs-only configuration-change guard
    description: h1lab direct configuration control
    conditions:
      - base = main
      - files ~= ^docs/
      - -mergify-configuration-changed
    actions:
      merge:
        method: squash
